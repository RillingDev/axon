"use strict";

const _window = window;
const _document = document;

const DEBOUNCE_TIMEOUT = 40; //event timeout in ms

const DOM_PREFIX = "x-";

export {
    _window,
    _document,

    DEBOUNCE_TIMEOUT,
    
    DOM_PREFIX
};
